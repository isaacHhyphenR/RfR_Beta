# lar_local_recruitment = {
# ###################unlocks decisions to recruit in eriador
	# recruit_eriador_agent = {

		# icon = GFX_decision_SWI_expand_covert_operations

		# available = {
			
		# }

		# visible = {
			# capital_scope = {
				# NOT = {
					# is_on_continent = eriador_continent
				# }
			# }
			# OR = {
				# any_owned_state = {
					# is_on_continent = eriador_continent
				# }
				# any_allied_country = {
					# any_owned_state = {
						# is_on_continent = eriador_continent
					# }
				# }
				# any_of_scopes = {
					# array = subjects
					# any_owned_state = {
						# is_on_continent = eriador_continent
					# }
				# }
			# }
		# }

		# fire_only_once = yes
		# cost = 50

		# modifier = {
			# civilian_factory_use = 3
		# }

		# days_remove = 90

		# remove_effect = {
			# set_country_flag = eriador_recruitment_unlocked
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }

	# recruit_in_eriador_state = {

		# icon = onmap_recruit_operative

		# available = {
			
		# }

		# state_target = yes
		# target_trigger = {
			# FROM = {
				# is_on_continent = eriador_continent
				# OR = {
					# AND = {
						# has_variable = historical_capital_for_country
						# var:historical_capital_for_country = {
							# exists = yes
							# has_capitulated = yes
							# NOT = { tag = ROOT }
						# }
					# }
					# AND = {
						# is_capital = yes
						# CONTROLLER = {
							# NOT = {
								# tag = ROOT
							# }
						# }
					# }
				# }
			# }
		# }
		# visible = {
			# OR = {
				# has_country_flag = eriador_recruitment_unlocked
				# capital_scope = { is_on_continent = eriador_continent }
			# }
			# NOT = { has_country_flag = eriador_recruitment_in_process }
		# }

		# fire_only_once = no
		# cost = 0

		# days_re_enable = 180
		# on_map_mode = map_only
		# complete_effect = {
			# set_country_flag = { flag = eriador_recruitment_in_process days = 180 value = 1 }
			# if = {
				# limit = {
					# num_of_operatives < num_operative_slots
				# }
				# custom_effect_tooltip = recruit_in_state_tt
			# }
			# else = {
				# custom_effect_tooltip = recruit_in_state_to_pool_tt
			# }
			# hidden_effect = {
				# create_operative_leader = {
					# bypass_recruitment = yes
					# nationalities = { FROM.OWNER }
					# portrait_tag_override = FROM.OWNER
				# }
			# }
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }
# ###################unlocks decisions to recruit in enedwaith
	# recruit_enedwaith_agent = {

		# icon = GFX_decision_SWI_expand_covert_operations

		# available = {
			
		# }

		# visible = {
			# capital_scope = {
				# NOT = {
					# is_greater_enedwaith_state = yes
				# }
			# }
			# OR = {
				# any_owned_state = {
					# is_greater_enedwaith_state = yes
				# }
				# any_allied_country = {
					# any_owned_state = {
						# is_greater_enedwaith_state = yes
					# }
				# }
				# any_of_scopes = {
					# array = subjects
					# any_owned_state = {
						# is_greater_enedwaith_state = yes
					# }
				# }
			# }
		# }

		# fire_only_once = yes
		# cost = 50

		# modifier = {
			# civilian_factory_use = 3
		# }

		# days_remove = 90

		# remove_effect = {
			# set_country_flag = enedwaith_recruitment_unlocked
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }

	# recruit_in_enedwaith_state = {

		# icon = onmap_recruit_operative

		# available = {
			
		# }

		# state_target = yes
		# target_trigger = {
			# FROM = {
				# is_greater_enedwaith_state = yes
				# OR = {
					# AND = {
						# has_variable = historical_capital_for_country
						# var:historical_capital_for_country = {
							# exists = yes
							# has_capitulated = yes
							# NOT = { tag = ROOT }
						# }
					# }
					# AND = {
						# is_capital = yes
						# CONTROLLER = {
							# NOT = {
								# tag = ROOT
							# }
						# }
					# }
				# }
			# }
		# }
		# visible = {
			# OR = {
				# has_country_flag = enedwaith_recruitment_unlocked
				# capital_scope = { is_greater_enedwaith_state = yes }
			# }
			# NOT = { has_country_flag = enedwaith_recruitment_in_process }
		# }

		# fire_only_once = no
		# cost = 0

		# days_re_enable = 180
		# on_map_mode = map_only
		# complete_effect = {
			# set_country_flag = { flag = enedwaith_recruitment_in_process days = 180 value = 1 }
			# if = {
				# limit = {
					# num_of_operatives < num_operative_slots
				# }
				# custom_effect_tooltip = recruit_in_enedwaith_state_tt
			# }
			# else = {
				# custom_effect_tooltip = recruit_in_state_to_pool_tt
			# }
			# hidden_effect = {##get operative with nationalities on all of this culture
				# if = { limit = { FROM.OWNER = { is_Andrast_tag = yes } }
					# create_operative_leader = {
						# bypass_recruitment = yes
						# nationalities = { ADR AND NEN PRI }
						# portrait_tag_override = FROM.OWNER
					# }
				# }
				# else_if = { limit = { FROM.OWNER = { is_Druwaith_Iaur_tag = yes } }
					# create_operative_leader = {
						# bypass_recruitment = yes
						# nationalities = { AKN DRU DWI IRN }
						# portrait_tag_override = FROM.OWNER
					# }
				# }
				# else_if = { limit = { FROM.OWNER = { is_Dunland_tag = yes } }
					# create_operative_leader = {
						# bypass_recruitment = yes
						# nationalities = { CGN DFN DTH DUN }
						# portrait_tag_override = FROM.OWNER
					# }
				# }
				# else_if = { limit = { FROM.OWNER = { is_Enedwaith_tag = yes } }
					# create_operative_leader = {
						# bypass_recruitment = yes
						# nationalities = { AGR COL HST STK }
						# portrait_tag_override = FROM.OWNER
					# }
				# }
				# else_if = { limit = { FROM.OWNER = { is_Eregion_tag = yes } }
					# create_operative_leader = {
						# bypass_recruitment = yes
						# nationalities = { ERG HOL MBL REE }
						# portrait_tag_override = FROM.OWNER
					# }
				# }
				# else_if = { limit = { FROM.OWNER = { is_White_Mountains_tag = yes } }
					# create_operative_leader = {
						# bypass_recruitment = yes
						# nationalities = { ANU HVK NIF TAW WHT }
						# portrait_tag_override = FROM.OWNER
					# }
				# }
			# }
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }
# ###################unlocks decisions to recruit in misty_mountains
	# recruit_misty_mountains_agent = {

		# icon = GFX_decision_SWI_expand_covert_operations

		# available = {
			
		# }

		# visible = {
			# capital_scope = {
				# NOT = {
					# is_on_continent = misty_mountains_continent
				# }
			# }
			# OR = {
				# any_owned_state = {
					# is_on_continent = misty_mountains_continent
				# }
				# any_allied_country = {
					# any_owned_state = {
						# is_on_continent = misty_mountains_continent
					# }
				# }
				# any_of_scopes = {
					# array = subjects
					# any_owned_state = {
						# is_on_continent = misty_mountains_continent
					# }
				# }
			# }
		# }

		# fire_only_once = yes
		# cost = 50

		# modifier = {
			# civilian_factory_use = 3
		# }

		# days_remove = 90

		# remove_effect = {
			# set_country_flag = misty_mountains_recruitment_unlocked
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }

	# recruit_in_misty_mountains_state = {

		# icon = onmap_recruit_operative

		# available = {
			
		# }

		# state_target = yes
		# target_trigger = {
			# FROM = {
				# is_on_continent = misty_mountains_continent
				# OR = {
					# AND = {
						# has_variable = historical_capital_for_country
						# var:historical_capital_for_country = {
							# exists = yes
							# has_capitulated = yes
							# NOT = { tag = ROOT }
						# }
					# }
					# AND = {
						# is_capital = yes
						# CONTROLLER = {
							# NOT = {
								# tag = ROOT
							# }
						# }
					# }
				# }
			# }
		# }
		# visible = {
			# OR = {
				# has_country_flag = misty_mountains_recruitment_unlocked
				# capital_scope = { is_on_continent = misty_mountains_continent }
			# }
			# NOT = { has_country_flag = misty_mountains_recruitment_in_process }
		# }

		# fire_only_once = no
		# cost = 0

		# days_re_enable = 180
		# on_map_mode = map_only
		# complete_effect = {
			# set_country_flag = { flag = misty_mountains_recruitment_in_process days = 180 value = 1 }
			# if = {
				# limit = {
					# num_of_operatives < num_operative_slots
				# }
				# custom_effect_tooltip = recruit_in_state_tt
			# }
			# else = {
				# custom_effect_tooltip = recruit_in_state_to_pool_tt
			# }
			# hidden_effect = {
				# create_operative_leader = {
					# bypass_recruitment = yes
					# nationalities = { FROM.OWNER }
					# portrait_tag_override = FROM.OWNER
				# }
			# }
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }
# ###################unlocks decisions to recruit in anduin
	# recruit_anduin_agent = {

		# icon = GFX_decision_SWI_expand_covert_operations

		# available = {
			
		# }

		# visible = {
			# capital_scope = {
				# NOT = {
					# is_on_continent = anduin_continent
				# }
			# }
			# OR = {
				# any_owned_state = {
					# is_on_continent = anduin_continent
				# }
				# any_allied_country = {
					# any_owned_state = {
						# is_on_continent = anduin_continent
					# }
				# }
				# any_of_scopes = {
					# array = subjects
					# any_owned_state = {
						# is_on_continent = anduin_continent
					# }
				# }
			# }
		# }

		# fire_only_once = yes
		# cost = 50

		# modifier = {
			# civilian_factory_use = 3
		# }

		# days_remove = 90

		# remove_effect = {
			# set_country_flag = anduin_recruitment_unlocked
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }

	# recruit_in_anduin_state = {

		# icon = onmap_recruit_operative

		# available = {
			
		# }

		# state_target = yes
		# target_trigger = {
			# FROM = {
				# is_on_continent = anduin_continent
				# OR = {
					# AND = {
						# has_variable = historical_capital_for_country
						# var:historical_capital_for_country = {
							# exists = yes
							# has_capitulated = yes
							# NOT = { tag = ROOT }
						# }
					# }
					# AND = {
						# is_capital = yes
						# CONTROLLER = {
							# NOT = {
								# tag = ROOT
							# }
						# }
					# }
				# }
			# }
		# }
		# visible = {
			# OR = {
				# has_country_flag = anduin_recruitment_unlocked
				# capital_scope = { is_on_continent = anduin_continent }
			# }
			# NOT = { has_country_flag = anduin_recruitment_in_process }
		# }

		# fire_only_once = no
		# cost = 0

		# days_re_enable = 180
		# on_map_mode = map_only
		# complete_effect = {
			# set_country_flag = { flag = anduin_recruitment_in_process days = 180 value = 1 }
			# if = {
				# limit = {
					# num_of_operatives < num_operative_slots
				# }
				# custom_effect_tooltip = recruit_in_state_tt
			# }
			# else = {
				# custom_effect_tooltip = recruit_in_state_to_pool_tt
			# }
			# hidden_effect = {
				# create_operative_leader = {
					# bypass_recruitment = yes
					# nationalities = { FROM.OWNER }
					# portrait_tag_override = FROM.OWNER
				# }
			# }
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }
# ###################unlocks decisions to recruit in rhovanion
	# recruit_rhovanion_agent = {

		# icon = GFX_decision_SWI_expand_covert_operations

		# available = {
			
		# }

		# visible = {
			# capital_scope = {
				# NOT = {
					# is_on_continent = rhovanion_continent
				# }
			# }
			# OR = {
				# any_owned_state = {
					# is_on_continent = rhovanion_continent
				# }
				# any_allied_country = {
					# any_owned_state = {
						# is_on_continent = rhovanion_continent
					# }
				# }
				# any_of_scopes = {
					# array = subjects
					# any_owned_state = {
						# is_on_continent = rhovanion_continent
					# }
				# }
			# }
		# }

		# fire_only_once = yes
		# cost = 50

		# modifier = {
			# civilian_factory_use = 3
		# }

		# days_remove = 90

		# remove_effect = {
			# set_country_flag = rhovanion_recruitment_unlocked
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }

	# recruit_in_rhovanion_state = {

		# icon = onmap_recruit_operative

		# available = {
			
		# }

		# state_target = yes
		# target_trigger = {
			# FROM = {
				# is_on_continent = rhovanion_continent
				# OR = {
					# AND = {
						# has_variable = historical_capital_for_country
						# var:historical_capital_for_country = {
							# exists = yes
							# has_capitulated = yes
							# NOT = { tag = ROOT }
						# }
					# }
					# AND = {
						# is_capital = yes
						# CONTROLLER = {
							# NOT = {
								# tag = ROOT
							# }
						# }
					# }
				# }
			# }
		# }
		# visible = {
			# OR = {
				# has_country_flag = rhovanion_recruitment_unlocked
				# capital_scope = { is_on_continent = rhovanion_continent }
			# }
			# NOT = { has_country_flag = rhovanion_recruitment_in_process }
		# }

		# fire_only_once = no
		# cost = 0

		# days_re_enable = 180
		# on_map_mode = map_only
		# complete_effect = {
			# set_country_flag = { flag = rhovanion_recruitment_in_process days = 180 value = 1 }
			# if = {
				# limit = {
					# num_of_operatives < num_operative_slots
				# }
				# custom_effect_tooltip = recruit_in_state_tt
			# }
			# else = {
				# custom_effect_tooltip = recruit_in_state_to_pool_tt
			# }
			# hidden_effect = {
				# create_operative_leader = {
					# bypass_recruitment = yes
					# nationalities = { FROM.OWNER }
					# portrait_tag_override = FROM.OWNER
				# }
			# }
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }
# ###################unlocks decisions to recruit in Gondor
	# recruit_gondor_agent = {

		# icon = GFX_decision_SWI_expand_covert_operations

		# available = {
			
		# }

		# visible = {
			# capital_scope = {
				# NOT = {
					# is_on_continent = gondor_continent
					# is_greater_enedwaith_state = no
				# }
			# }
			# OR = {
				# any_owned_state = {
					# is_on_continent = gondor_continent
					# is_greater_enedwaith_state = no
				# }
				# any_allied_country = {
					# any_owned_state = {
						# is_on_continent = gondor_continent
					# is_greater_enedwaith_state = no
					# }
				# }
				# any_of_scopes = {
					# array = subjects
					# any_owned_state = {
						# is_on_continent = gondor_continent
						# is_greater_enedwaith_state = no
					# }
				# }
			# }
		# }

		# fire_only_once = yes
		# cost = 50

		# modifier = {
			# civilian_factory_use = 3
		# }

		# days_remove = 90

		# remove_effect = {
			# set_country_flag = gondor_recruitment_unlocked
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }

	# recruit_in_gondor_state = {

		# icon = onmap_recruit_operative

		# available = {
			
		# }

		# state_target = yes
		# target_trigger = {
			# FROM = {
				# is_on_continent = gondor_continent
				# OR = {
					# AND = {
						# has_variable = historical_capital_for_country
						# var:historical_capital_for_country = {
							# exists = yes
							# has_capitulated = yes
							# NOT = { tag = ROOT }
						# }
					# }
					# AND = {
						# is_capital = yes
						# CONTROLLER = {
							# NOT = {
								# tag = ROOT
							# }
						# }
					# }
				# }
			# }
		# }
		# visible = {
			# OR = {
				# has_country_flag = gondor_recruitment_unlocked
				# capital_scope = { is_on_continent = gondor_continent }
			# }
			# NOT = { has_country_flag = gondor_recruitment_in_process }
		# }

		# fire_only_once = no
		# cost = 0

		# days_re_enable = 180
		# on_map_mode = map_only
		# complete_effect = {
			# set_country_flag = { flag = gondor_recruitment_in_process days = 180 value = 1 }
			# if = {
				# limit = {
					# num_of_operatives < num_operative_slots
				# }
				# custom_effect_tooltip = recruit_in_state_tt
			# }
			# else = {
				# custom_effect_tooltip = recruit_in_state_to_pool_tt
			# }
			# hidden_effect = {
				# create_operative_leader = {
					# bypass_recruitment = yes
					# nationalities = { FROM.OWNER }
					# portrait_tag_override = FROM.OWNER
				# }
			# }
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }
# ###################unlocks decisions to recruit in mordor
	# recruit_mordor_agent = {

		# icon = GFX_decision_SWI_expand_covert_operations

		# available = {
			
		# }

		# visible = {
			# capital_scope = {
				# NOT = {
					# is_on_continent = mordor_continent
				# }
			# }
			# OR = {
				# any_owned_state = {
					# is_on_continent = mordor_continent
				# }
				# any_allied_country = {
					# any_owned_state = {
						# is_on_continent = mordor_continent
					# }
				# }
				# any_of_scopes = {
					# array = subjects
					# any_owned_state = {
						# is_on_continent = mordor_continent
					# }
				# }
			# }
		# }

		# fire_only_once = yes
		# cost = 50

		# modifier = {
			# civilian_factory_use = 3
		# }

		# days_remove = 90

		# remove_effect = {
			# set_country_flag = mordor_recruitment_unlocked
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }

	# recruit_in_mordor_state = {

		# icon = onmap_recruit_operative

		# available = {
			
		# }

		# state_target = yes
		# target_trigger = {
			# FROM = {
				# is_on_continent = mordor_continent
				# OR = {
					# AND = {
						# has_variable = historical_capital_for_country
						# var:historical_capital_for_country = {
							# exists = yes
							# has_capitulated = yes
							# NOT = { tag = ROOT }
						# }
					# }
					# AND = {
						# is_capital = yes
						# CONTROLLER = {
							# NOT = {
								# tag = ROOT
							# }
						# }
					# }
				# }
			# }
		# }
		# visible = {
			# OR = {
				# has_country_flag = mordor_recruitment_unlocked
				# capital_scope = { is_on_continent = mordor_continent }
			# }
			# NOT = { has_country_flag = mordor_recruitment_in_process }
		# }

		# fire_only_once = no
		# cost = 0

		# days_re_enable = 180
		# on_map_mode = map_only
		# complete_effect = {
			# set_country_flag = { flag = mordor_recruitment_in_process days = 180 value = 1 }
			# if = {
				# limit = {
					# num_of_operatives < num_operative_slots
				# }
				# custom_effect_tooltip = recruit_in_state_tt
			# }
			# else = {
				# custom_effect_tooltip = recruit_in_state_to_pool_tt
			# }
			# hidden_effect = {
				# create_operative_leader = {
					# bypass_recruitment = yes
					# nationalities = { FROM.OWNER }
					# portrait_tag_override = FROM.OWNER
				# }
			# }
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }
# ###################unlocks decisions to recruit in harad
	# recruit_harad_agent = {

		# icon = GFX_decision_SWI_expand_covert_operations

		# available = {
			
		# }

		# visible = {
			# capital_scope = {
				# NOT = {
					# is_on_continent = harad_continent
				# }
			# }
			# OR = {
				# any_owned_state = {
					# is_on_continent = harad_continent
				# }
				# any_allied_country = {
					# any_owned_state = {
						# is_on_continent = harad_continent
					# }
				# }
				# any_of_scopes = {
					# array = subjects
					# any_owned_state = {
						# is_on_continent = harad_continent
					# }
				# }
			# }
		# }

		# fire_only_once = yes
		# cost = 50

		# modifier = {
			# civilian_factory_use = 3
		# }

		# days_remove = 90

		# remove_effect = {
			# set_country_flag = harad_recruitment_unlocked
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }

	# recruit_in_harad_state = {

		# icon = onmap_recruit_operative

		# available = {
			
		# }

		# state_target = yes
		# target_trigger = {
			# FROM = {
				# is_on_continent = harad_continent
				# OR = {
					# AND = {
						# has_variable = historical_capital_for_country
						# var:historical_capital_for_country = {
							# exists = yes
							# has_capitulated = yes
							# NOT = { tag = ROOT }
						# }
					# }
					# AND = {
						# is_capital = yes
						# CONTROLLER = {
							# NOT = {
								# tag = ROOT
							# }
						# }
					# }
				# }
			# }
		# }
		# visible = {
			# OR = {
				# has_country_flag = harad_recruitment_unlocked
				# capital_scope = { is_on_continent = harad_continent }
			# }
			# NOT = { has_country_flag = harad_recruitment_in_process }
		# }

		# fire_only_once = no
		# cost = 0

		# days_re_enable = 180
		# on_map_mode = map_only
		# complete_effect = {
			# set_country_flag = { flag = harad_recruitment_in_process days = 180 value = 1 }
			# if = {
				# limit = {
					# num_of_operatives < num_operative_slots
				# }
				# custom_effect_tooltip = recruit_in_state_tt
			# }
			# else = {
				# custom_effect_tooltip = recruit_in_state_to_pool_tt
			# }
			# hidden_effect = {
				# create_operative_leader = {
					# bypass_recruitment = yes
					# nationalities = { FROM.OWNER }
					# portrait_tag_override = FROM.OWNER
				# }
			# }
		# }

		# ai_will_do = {
			# base = 0
		# }
	# }
# }
